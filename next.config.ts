import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['picsum.photos','via.placeholder.com'], // Picsum photos domaini eklendi
  },
};

export default nextConfig;
